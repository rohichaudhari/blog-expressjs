<!DOCTYPE html>
<html>

<head>
    <title> BLog List</title>
    <style>
        table ,td,th,tr{
          border:1px solid black;
         width: 700px;
        }
        table{
            align-items: center;
            text-align: center;
            justify-content: center;
        }
          
        
      
    </style>
</head>
<script src="https://cdn.tailwindcss.com"></script>
<body class="bg-gray-400 text-white flex justify-center">
    <div>
    <div class="bg-blue-700 h-[400px] w-[700px] text-white border-2 border-white flex justify-center ">
        <form action="/api/profile/" method="post" class="mt-10">
            <h2 class="text-4xl flex justify-center underline" > ADD BLOG LIST</h2><br>
            <label class="text-2xl" for="title">Title:</label><br>
            <input class="w-80 h-10" type="text" id="title" name="title" placeholder="Enter title..."><br><br>
            <label class="text-2xl" for="description">Description:</label><br>
            <input  class="w-80 h-10" type="text" id="desc" name="description" placeholder="write description....."><br><br>
            
            <button class="rounded-lg border-2 font-bold w-40 h-10 flex justify-center align-center ml-20 text-center" type="submit">
                <p class="mt-2">ADD BLOG</p>
            </button>
        </form>
    </div>

    <table class="mt-10">
        <tr>
            <th>Sr. No</th>
            <th>Title</th>
            <th>Description</th>
            <th>Date</th>
            <th>Edit</th>
            <th>Delete</th>
            
        </tr>

        <% for(let i=0 ;i < profile.length; i++) {%>
            <tr>
                <td>
                    <%= i + 1 %>
                </td>
                <td>
                    <%= profile[i].title %>
                </td>
                <td>
                    <%= profile[i].description %>
                </td>
                <td>
                    <%= profile[i].createAt.toDateString() %>
                </td>
                <td>
                   <div class="flex ">

                       <form action="/api/profile/<%= profile[i]._id %>/edit" method="get" >
                           <button class="bg-green-700 w-20 rounded-sm text-white ml-10 p-1 " type="submit">Edit</button>
                       </form>
                    </td>
                <td>
                       <form action="/api/profile/<%= profile[i]._id %>" method="post">
                           <input type="hidden" name="_method" value="DELETE">
                           <button class="bg-red-700 rounded-sm text-white p-1" type="submit" onclick="return confirm('Are you sure you want to delete this profile?');">Delete</button>
                       </form>
                   </div>
                </td>
            </tr>
            <% } %>
    </table>
</div>
</body>